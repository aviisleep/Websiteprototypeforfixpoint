
  # Website Prototype for FixPoint

  This is a code bundle for Website Prototype for FixPoint. The original project is available at https://www.figma.com/design/J9O0765KfXTEDD2PTcUy6b/Website-Prototype-for-FixPoint.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  